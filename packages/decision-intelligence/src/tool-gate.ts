export type ToolGateInput={action:string;productId?:string;productExists:boolean;productValid:boolean;actionAllowed:boolean;liveState:string;emergencyStopped:boolean;humanTakeover:boolean;policyAllowed:boolean};
export type ToolGateResult={allowed:true;reasonCode:'ALLOWED'}|{allowed:false;reasonCode:'UNKNOWN_ACTION'|'PRODUCT_NOT_FOUND'|'INVALID_PRODUCT'|'ACTION_NOT_ALLOWED'|'RUNTIME_BLOCKED'|'EMERGENCY_STOPPED'|'HUMAN_TAKEOVER'|'POLICY_REJECTED'};
const allowedActions=new Set(['SHOW_PRODUCT','HIDE_PRODUCT','SET_SCENE','SET_GESTURE','PAUSE_AI','RESUME_AI']);
export class ToolGate {evaluate(input:ToolGateInput):ToolGateResult{if(input.emergencyStopped||input.liveState==='EMERGENCY_STOPPED')return{allowed:false,reasonCode:'EMERGENCY_STOPPED'};if(input.humanTakeover||input.liveState==='HUMAN_TAKEOVER')return{allowed:false,reasonCode:'HUMAN_TAKEOVER'};if(!input.policyAllowed)return{allowed:false,reasonCode:'POLICY_REJECTED'};if(!allowedActions.has(input.action))return{allowed:false,reasonCode:'UNKNOWN_ACTION'};if(!input.actionAllowed)return{allowed:false,reasonCode:'ACTION_NOT_ALLOWED'};if(input.action==='SHOW_PRODUCT'){if(!input.productId||!input.productExists)return{allowed:false,reasonCode:'PRODUCT_NOT_FOUND'};if(!input.productValid)return{allowed:false,reasonCode:'INVALID_PRODUCT'}}if(!['RUNNING','PAUSED'].includes(input.liveState))return{allowed:false,reasonCode:'RUNTIME_BLOCKED'};return{allowed:true,reasonCode:'ALLOWED'}}}
