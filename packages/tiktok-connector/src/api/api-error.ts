import type {TikTokError,TikTokErrorCode} from '../types.js';
export class TikTokApiError extends Error {constructor(readonly detail:TikTokError){super(detail.message);this.name='TikTokApiError'}}
export function apiError(code:TikTokErrorCode,source:string,retryAfterMs?:number):TikTokApiError{return new TikTokApiError({code,message:messages[code],recoverable:['NETWORK_ERROR','TIMEOUT','RATE_LIMITED','TOKEN_EXPIRED','TOKEN_REFRESH_FAILED'].includes(code),...(retryAfterMs===undefined?{}:{retryAfterMs}),source})}
const messages:Record<TikTokErrorCode,string>={NETWORK_ERROR:'TikTok service is unreachable.',TIMEOUT:'TikTok request timed out.',RATE_LIMITED:'TikTok rate limit reached.',UNAUTHORIZED:'TikTok authorization is no longer valid.',FORBIDDEN:'TikTok did not authorize this operation.',INVALID_REQUEST:'TikTok rejected the request.',NOT_APPROVED:'This TikTok capability is not approved for the app.',NOT_SUPPORTED:'This TikTok capability is not supported by the configured API.',INVALID_RESPONSE:'TikTok returned an invalid response.',TOKEN_EXPIRED:'TikTok access token expired.',TOKEN_REFRESH_FAILED:'TikTok token refresh failed; reauthorization may be required.',STATE_MISMATCH:'TikTok authorization state validation failed.',PKCE_ERROR:'TikTok PKCE validation failed.',USER_CANCELLED:'TikTok authorization was cancelled.',UNKNOWN:'TikTok operation failed.'};
