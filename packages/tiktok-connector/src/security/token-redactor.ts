const secretKey=/(?:access.?token|refresh.?token|client.?secret|authorization.?code|code.?verifier|authorization|password|cookie|secret)/i;
export function redactSecrets(value:unknown):unknown{if(Array.isArray(value))return value.map(redactSecrets);if(!value||typeof value!=='object')return typeof value==='string'?value.replace(/(Bearer\s+)[A-Za-z0-9._~-]+/gi,'$1[REDACTED]').replace(/(code|access_token|refresh_token|client_secret|code_verifier)=([^&\s]+)/gi,'$1=[REDACTED]'):value;return Object.fromEntries(Object.entries(value).map(([key,item])=>[key,secretKey.test(key)?'[REDACTED]':redactSecrets(item)]))}
export function safeTikTokError(code:string):string{const allowed=new Set(['NETWORK_ERROR','TIMEOUT','RATE_LIMITED','UNAUTHORIZED','FORBIDDEN','INVALID_REQUEST','NOT_APPROVED','NOT_SUPPORTED','INVALID_RESPONSE','TOKEN_EXPIRED','TOKEN_REFRESH_FAILED','STATE_MISMATCH','PKCE_ERROR','USER_CANCELLED']);return allowed.has(code)?code:'UNKNOWN'}
