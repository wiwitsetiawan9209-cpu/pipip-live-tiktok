import type {TimeAwareness} from './types.js';
export interface PeriodWindow {period:TimeAwareness['period'];startHour:number;endHour:number}
export const DEFAULT_PERIODS:PeriodWindow[]=[{period:'MORNING',startHour:5,endHour:11},{period:'DAY',startHour:11,endHour:15},{period:'AFTERNOON',startHour:15,endHour:18},{period:'EVENING',startHour:18,endHour:23},{period:'NIGHT',startHour:23,endHour:5}];
export function periodForHour(hour:number,windows:PeriodWindow[]=DEFAULT_PERIODS):TimeAwareness['period']{const normalized=((hour%24)+24)%24;return windows.find(w=>w.startHour<w.endHour?normalized>=w.startHour&&normalized<w.endHour:normalized>=w.startHour||normalized<w.endHour)?.period??'NIGHT'}
export class TimeAwarenessService {constructor(private readonly timezone='Asia/Jakarta',private readonly now=()=>Date.now(),private readonly windows:PeriodWindow[]=DEFAULT_PERIODS){}snapshot(sessionStartedAt:number):TimeAwareness{const instant=this.now();const date=new Date(instant);const parts=new Intl.DateTimeFormat('en-US',{timeZone:this.timezone,year:'numeric',month:'2-digit',day:'2-digit',weekday:'long',hour:'2-digit',minute:'2-digit',hourCycle:'h23'}).formatToParts(date);const part=(name:string)=>parts.find(p=>p.type===name)?.value??'';const hour=Number(part('hour'));return{date:`${part('year')}-${part('month')}-${part('day')}`,dayOfWeek:part('weekday'),time:`${part('hour')}:${part('minute')}`,timezone:this.timezone,period:periodForHour(hour,this.windows),sessionStartedAt,sessionDurationMs:Math.max(0,instant-sessionStartedAt)}}}
