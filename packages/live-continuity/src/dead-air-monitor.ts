import type {DeadAirInput,DeadAirStatus} from './types.js';
import type {ContinuityThresholds} from './types.js';
export const DEFAULT_CONTINUITY_THRESHOLDS:ContinuityThresholds={prepareMs:15_000,warningMs:30_000,interventionMs:45_000,emergencyMs:60_000};
export class DeadAirMonitor {constructor(private thresholds:ContinuityThresholds=DEFAULT_CONTINUITY_THRESHOLDS){}getDeadAirStatus(input:DeadAirInput):DeadAirStatus{if(input.stopped)return'NORMAL';if(input.paused)return'EXEMPT_PAUSED';if(input.humanTakeover)return'EXEMPT_HUMAN_TAKEOVER';if(input.activity==='MUSIC_SEGMENT'&&input.musicPlaying)return'EXEMPT_MUSIC';if(input.speechInProgress)return'NORMAL';if(input.awaitingAudience)return'NORMAL';if(!input.hostExpectedToSpeak)return'NORMAL';const reference=Math.max(input.lastHostSpeechAt??0,input.lastAudienceEventAt??0);const quiet=Math.max(0,input.now-reference);if(quiet>=this.thresholds.emergencyMs)return'EMERGENCY';if(quiet>=this.thresholds.interventionMs)return'INTERVENTION_REQUIRED';if(quiet>=this.thresholds.warningMs)return'WARNING';if(quiet>=this.thresholds.prepareMs)return'PREPARING';return'NORMAL'}}
